// const btnAnswer = document.querySelectorAll('.btn-answer');
// // console.log(btnAnswer);

// for (let i = 0; i < btnAnswer.length; i++) {
// 	btnAnswer[i].addEventListener('click', () => {
// 		if (randomQuestions.indexOf(randomQuestions[i]) + 1 === randomQuestions[i].ansIndex) {
// 			btnAnswer[i].style.background = 'green';
// 		}
// 	});
// }
